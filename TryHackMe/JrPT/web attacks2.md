Internet Information Services (IIS) is Microsoft's web server platform, it hosts services, applications, websites

IIS fingerprinting version

WebDAV extension file management

curl -X OPTIONS http://10.112.165.195 -sv 2>&1 | grep -E "Allow:|DAV:"   check if webDAV is active

PUT a test file GET to view the response

8.3 Short Filename Problem = BACKUP~1  ADMINI~1, if it returns a different 404 there must be a file starting with that name

iis_shortname_scan.py = python tool that reconstructs shortnames

shell upload = if webDAV is enabled, write permissions, valid credentials, script execute is set

ASPX shells = aspnet file on a web server, accepts input via http
